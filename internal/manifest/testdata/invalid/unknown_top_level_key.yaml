name: "x"
version: "0.1.0"
manifest_version: 1
base_url: "https://api.example.com"
actions:
  list:
    role: source
    method: GET
    path: "/items"

# Unkown top-level key that would invalidate the manifest
naem: "typo"