name: "x"
version: "0.1.0"
manifest_version: 1
base_url: "https://api.example.com"
retry:
  max_attempts: 3
  # Only exponential, linear or fixed.
  backoff: chaotic
actions:
  list:
    role: source
    method: GET
    path: "/items"
