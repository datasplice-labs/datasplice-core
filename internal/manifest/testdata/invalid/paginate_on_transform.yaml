name: "x"
version: "0.1.0"
manifest_version: 1
base_url: "https://api.example.com"
actions:
  list:
    role: transform
    method: POST
    path: "/items"
    # Only source actions paginate.
    paginate:
      style: page
      param: page
