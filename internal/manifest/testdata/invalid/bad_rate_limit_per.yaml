name: "x"
version: "0.1.0"
manifest_version: 1
base_url: "https://api.example.com"
rate_limit:
  requests: 10
  # Only second, minute or hour.
  per: fortnight
actions:
  list:
    role: source
    method: GET
    path: "/items"
