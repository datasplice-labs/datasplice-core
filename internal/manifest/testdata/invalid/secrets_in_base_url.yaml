name: "x"
version: "0.1.0"
manifest_version: 1
# This is invalid because secrets cannot be used in the base_url field.
base_url: "https://{{ secrets.SUBDOMAIN }}.example.com"
secrets:
  SUBDOMAIN:
    description: "subdomain"
actions:
  list:
    role: source
    method: GET
    path: "/items"
