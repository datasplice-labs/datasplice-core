name: "x"
version: "0.1.0"
manifest_version: 1
base_url: "https://api.example.com"
actions:
  list:
    # This is invalid because action can only be ["source", "sink", "transform"].
    role: destroyer
    method: GET
    path: "/items"
