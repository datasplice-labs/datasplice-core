name: "x"
version: "0.1.0"
manifest_version: 1
base_url: "https://api.example.com"
actions:
  list:
    role: source
    method: GET
    path: "/items"
    paginate:
      # Valid style in the spec, but not built yet.
      style: offset
      param: offset
