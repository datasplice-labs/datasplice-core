name: "x"
version: "0.1.0"
manifest_version: 1
base_url: "https://api.example.com"
actions:
  list:
    role: source
    method: GET
    path: "/items"

# State is reserved and currently cannot be used in a manifest.
# This is invalid because state is reserved.
state:
  cursor: "some-field"